project('inet_cmds', 'c')

executable('ifconfig', 'ifconfig.c',
    install: true,
    install_dir: get_option('sbindir'),
)

executable('route', 'route.c',
    install: true,
    install_dir: get_option('sbindir'),
)
