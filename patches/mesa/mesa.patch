diff -ru mesa-21.3.9/meson.build mesa/meson.build
--- mesa-21.3.9/meson.build	2022-06-08 19:13:35.000000000 +0100
+++ mesa/meson.build	2025-12-31 19:53:38.069654084 +0000
@@ -176,7 +176,7 @@
 # Only build shared_glapi if at least one OpenGL API is enabled
 with_shared_glapi = with_shared_glapi and with_any_opengl

-system_has_kms_drm = ['openbsd', 'netbsd', 'freebsd', 'gnu/kfreebsd', 'dragonfly', 'linux', 'sunos'].contains(host_machine.system())
+system_has_kms_drm = ['openbsd', 'netbsd', 'freebsd', 'gnu/kfreebsd', 'dragonfly', 'linux', 'sunos', 'keyronex'].contains(host_machine.system())

 dri_drivers = get_option('dri-drivers')
 if dri_drivers.contains('auto')
diff -ru mesa-21.3.9/src/util/build_id.c mesa/src/util/build_id.c
--- mesa-21.3.9/src/util/build_id.c	2022-06-08 19:13:35.000000000 +0100
+++ mesa/src/util/build_id.c	2025-12-31 19:58:33.239767700 +0000
@@ -21,6 +21,8 @@
  * IN THE SOFTWARE.
  */

+#define _GNU_SOURCE
+
 #ifdef HAVE_DL_ITERATE_PHDR
 #include <dlfcn.h>
 #include <link.h>
diff -ru mesa-21.3.9/src/util/debug.c mesa/src/util/debug.c
--- mesa-21.3.9/src/util/debug.c	2022-06-08 19:13:35.000000000 +0100
+++ mesa/src/util/debug.c	2025-12-31 19:53:38.069654084 +0000
@@ -23,6 +23,7 @@

 #include <errno.h>
 #include <string.h>
+#include <strings.h>
 #include "debug.h"
 #include "u_string.h"

diff -ru mesa-21.3.9/src/util/detect_os.h mesa/src/util/detect_os.h
--- mesa-21.3.9/src/util/detect_os.h	2022-06-08 19:13:35.000000000 +0100
+++ mesa/src/util/detect_os.h	2025-12-31 19:53:38.069654084 +0000
@@ -81,6 +81,11 @@
 #define DETECT_OS_UNIX 1
 #endif

+#if defined(__keyronex__)
+#define DETECT_OS_KEYRONEX 1
+#define DETECT_OS_UNIX 1
+#endif
+

 /*
  * Make sure DETECT_OS_* are always defined, so that they can be used with #if
@@ -127,5 +132,8 @@
 #ifndef DETECT_OS_WINDOWS
 #define DETECT_OS_WINDOWS 0
 #endif
+#ifndef DETECT_OS_KEYRONEX
+#define DETECT_OS_KEYRONEX 0
+#endif

 #endif /* DETECT_OS_H */
diff -ru mesa-21.3.9/src/util/disk_cache.h mesa/src/util/disk_cache.h
--- mesa-21.3.9/src/util/disk_cache.h	2022-06-08 19:13:35.000000000 +0100
+++ mesa/src/util/disk_cache.h	2025-12-31 19:58:40.019781965 +0000
@@ -25,6 +25,7 @@
 #define DISK_CACHE_H

 #ifdef HAVE_DLFCN_H
+#define _GNU_SOURCE
 #include <dlfcn.h>
 #include <stdio.h>
 #include "util/build_id.h"
diff -ru mesa-21.3.9/src/util/os_misc.c mesa/src/util/os_misc.c
--- mesa-21.3.9/src/util/os_misc.c	2022-06-08 19:13:35.000000000 +0100
+++ mesa/src/util/os_misc.c	2025-12-31 19:53:38.069654084 +0000
@@ -57,7 +57,7 @@
 #  include <unistd.h>
 #  include <log/log.h>
 #  include <cutils/properties.h>
-#elif DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD
+#elif DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_KEYRONEX
 #  include <unistd.h>
 #elif DETECT_OS_OPENBSD || DETECT_OS_FREEBSD
 #  include <sys/resource.h>
@@ -223,7 +223,7 @@
 bool
 os_get_total_physical_memory(uint64_t *size)
 {
-#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD
+#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_KEYRONEX
    const long phys_pages = sysconf(_SC_PHYS_PAGES);
    const long page_size = sysconf(_SC_PAGE_SIZE);

diff -ru mesa-21.3.9/src/util/os_time.c mesa/src/util/os_time.c
--- mesa-21.3.9/src/util/os_time.c	2022-06-08 19:13:35.000000000 +0100
+++ mesa/src/util/os_time.c	2025-12-31 19:53:38.069654084 +0000
@@ -53,7 +53,7 @@
 int64_t
 os_time_get_nano(void)
 {
-#if DETECT_OS_LINUX || DETECT_OS_BSD
+#if DETECT_OS_LINUX || DETECT_OS_BSD || DETECT_OS_KEYRONEX || DETECT_OS_KEYRONEX

    struct timespec tv;
    clock_gettime(CLOCK_MONOTONIC, &tv);
diff -ru mesa-21.3.9/src/util/u_printf.h mesa/src/util/u_printf.h
--- mesa-21.3.9/src/util/u_printf.h	2022-06-08 19:13:35.000000000 +0100
+++ mesa/src/util/u_printf.h	2025-12-31 19:53:38.069654084 +0000
@@ -22,6 +22,8 @@
 #ifndef U_PRINTF_H
 #define U_PRINTF_H

+#include <stdarg.h>
+
 #ifdef __cplusplus

 #include <string>
diff -ru mesa-21.3.9/src/util/u_thread.h mesa/src/util/u_thread.h
--- mesa-21.3.9/src/util/u_thread.h	2022-06-08 19:13:35.000000000 +0100
+++ mesa/src/util/u_thread.h	2025-12-31 19:53:38.069654084 +0000
@@ -129,7 +129,7 @@
 static inline void u_thread_setname( const char *name )
 {
 #if defined(HAVE_PTHREAD)
-#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS
+#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_KEYRONEX
    int ret = pthread_setname_np(pthread_self(), name);
    if (ret == ERANGE) {
       char buf[16];
@@ -242,7 +242,7 @@
 static inline int64_t
 util_thread_get_time_nano(thrd_t thread)
 {
-#if defined(HAVE_PTHREAD) && !defined(__APPLE__) && !defined(__HAIKU__)
+#if defined(HAVE_PTHREAD) && !defined(__APPLE__) && !defined(__HAIKU__) && !defined(__keyronex__)
    struct timespec ts;
    clockid_t cid;
